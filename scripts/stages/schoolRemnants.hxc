package funkin.remnants.stages;

class SchoolRemnantsStage extends Stage
{
  function new()
  {
    super('schoolRemnants');
  }

  function buildStage()
  {
    super.buildStage();

    if (PlayState.instance?.currentSong?.id?.toLowerCase() == "roses")
    {
      getNamedProp('freaks')?.idleSuffix = '-scared';
    }
    else
    {
      getNamedProp('freaks')?.idleSuffix = '';
    }
  }
}
